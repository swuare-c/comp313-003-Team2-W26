{
  "name": "listening-companion-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd react-client && npm install && npm run build",
    "dev:backend": "cd express-server && npm run dev",
    "dev:frontend": "cd react-client && npm run dev"
  }
}